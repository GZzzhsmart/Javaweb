/* Note:Your choice is C IDE */
#include "stdio.h"
void main()
{	//2、编写程序，统计用户输入的字符数，按回车键结束。
  int a;
  char ch;
  printf("请输入一串字符");
 for(a=0;a<=
 ;a++){
  	scanf("%c",&ch);
  	if(ch=='\n')
  	break;
  	 	
  } printf("一共%d个字符",a); 
}