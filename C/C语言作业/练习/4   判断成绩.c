/* Note:Your choice is C IDE */
#include "stdio.h"
void main()
{
   /*
   4、循环输入5个学生的成绩，并计算出最高分、最低分、平均分  
   */ 
   int max1,min,average,p,a1,a2,a3,a4,a5; 
   printf("请输入五个整数（用空格隔开）");
   scanf("%d,%d,%d,%d,%d",&a1,&a2,&a3,&a4,&a5);
   max=a1>a2?a1:a2;
   max=max>a3?max:a3;
   max=p;
   printf("%d",p);
   
}